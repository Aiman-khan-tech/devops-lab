🐳 Docker Commands :

Build image : docker build -t my-app .

Run container : docker run -d -p 8080:80 my-app

Stop container : docker stop <container_id>

Remove container : docker rm <container_id>

Remove image : docker rmi <image_id>
